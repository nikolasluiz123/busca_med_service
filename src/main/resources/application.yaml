ktor:
  application:
    modules:
      - br.com.buscamed.ApplicationKt.module
  deployment:
    port: 8080

buscamed:
  gcp:
    project_id: ${GCP_PROJECT_ID}
    service_audience: ${GCP_SERVICE_AUDIENCE}
    region: "global"